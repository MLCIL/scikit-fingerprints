version: 2
formats: [htmlzip]

build:
  os: ubuntu-24.04
  tools:
    python: "3.10"

python:
  install:
    - method: uv
      command: sync
      groups:
        - docs

sphinx:
   configuration: docs/conf.py
